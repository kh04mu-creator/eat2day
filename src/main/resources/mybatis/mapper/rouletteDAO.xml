<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.study.springboot.dao.rouletteDAO">

  <select id="selectAll" resultType="com.study.springboot.dto.rouletteDTO">
    SELECT ro_no, ro_keyword
    FROM roulette
    ORDER BY ro_no desc
  </select>

  <select id="selectRandom" resultType="com.study.springboot.dto.rouletteDTO">
    SELECT ro_no, ro_keyword
    FROM (
      SELECT ro_no, ro_keyword
      FROM roulette
      ORDER BY DBMS_RANDOM.VALUE
    )
    WHERE ROWNUM = 1
  </select>

  <!-- ✅ 추가 -->
  <insert id="insert">
    INSERT INTO roulette(ro_no, ro_keyword)
    VALUES ('R' || LPAD(ROULETTE_SEQ.NEXTVAL, 5, '0'), #{ro_keyword})
  </insert>

  <!-- ✅ 수정 -->
  <update id="update">
    UPDATE roulette
    SET ro_keyword = #{ro_keyword}
    WHERE ro_no = #{ro_no}
  </update>

  <!-- ✅ 삭제 -->
  <delete id="delete">
    DELETE FROM roulette
    WHERE ro_no = #{ro_no}
  </delete>

</mapper>
